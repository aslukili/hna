import { TimeAgoFixPipe } from './time-ago-fix.pipe';

describe('TimeAgoFixPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeAgoFixPipe();
    expect(pipe).toBeTruthy();
  });
});
